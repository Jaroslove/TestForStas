/**
 * For test, you need write all methods to all tests in the project will be completed
 */
public class Main {
    private final int[] justForTest = {1, 3}; // it's a stub for methods

    //the method must be sum two values
    public int Add(int x, int y) {
        int sum = x + y;
        return  sum;
    }

    //gets all even number of the array
    public int[] GetEvenNumber(int[] array) {
        return justForTest;
    }

    //gets all even and positive number of the array
    public int[] GetEvenPositiveNumber(int[] array) {
        return justForTest;
    }

    //multiples each element of first array by second and save it in another array
    public int[] GetMultipliedArray(int[] firstArray, int[] secondArray) {
        return justForTest;
    }

    //ADVANCED TEST, you need find all element of first element which are contained in second array,
    // and put them into another array
    public int[] AllElementContainsInSecond(int[] first, int[] second) {
        return justForTest;
    }
}
